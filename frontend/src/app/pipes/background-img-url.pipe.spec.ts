import { BackgroundImgUrlPipe } from './background-img-url.pipe';

describe('BackgroundImgUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new BackgroundImgUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
